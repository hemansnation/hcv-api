# Python
python --version          # 3.11.x ✓
pip --version             # pip 23.x ✓

# Node (for Claude Code)
node --version            # v20.x.x ✓
claude --version          # Claude Code version ✓

# Database
psql hcv_db -c "SELECT 1;" # (1 row) ✓

# Redis
redis-cli ping            # PONG ✓

# Git
git status                # On branch main ✓